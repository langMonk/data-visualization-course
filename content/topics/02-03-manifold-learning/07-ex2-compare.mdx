**Compare dimensionality reduction methods**:
- Experiment with different hyperparameters for each method (perplexity for t-SNE, n_neighbors for UMAP, etc.)
- Generate visualizations of the roulade with different parameter settings
- Evaluate which method best preserves the spiral structure