### Introduction

Kernel PCA extends traditional PCA to handle nonlinear dimensionality reduction by implicitly mapping data into a higher-dimensional feature space.

While standard PCA finds principal components in the original feature space, Kernel PCA operates in a transformed space $\mathcal{F}$ using a nonlinear mapping function $\phi: \mathbb{R}^p \rightarrow \mathcal{F}$.

Instead of computing this mapping explicitly, Kernel PCA uses the "kernel trick" by defining a kernel function:
